# Name:   

drawrandomCircles();

## Examples:
drawrandomCircles(){
  noStroke();
  fill(random(0, 255), random(0,0), random(0,255),random(0,255)); 
  ellipse(random(0, 800), random(0, 600), diam, diam);
  return(diam);
}
## Description:
this function will draw random circles of different sizes depending on the mouse location. 
it will also print the diameters in the line. 

## Syntax:
drawrandomCircles(); 

##Parameters: 
diam -- float diam meaning diameter

##Returns:
return(diam);
it returns the value of the random diameters

##Other notes:
Anything else?
